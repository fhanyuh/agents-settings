# Mock Data

Per `AGENTS.md` §5 (Mockup Data & Demo/Live Mode), all mock/sample data used by the
Demo-mode mock API layer lives in this folder, separate from UI components and
styling. Add one file per resource/entity (e.g. `users.json`, `orders.json`) that the
mock API layer reads from to mirror the real backend's contract.
